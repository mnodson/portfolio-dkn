on:
  push:
    branches:
      - main

name: Build Angular

permissions:
  contents: write

jobs:
  
  # Build job
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.13.1]

    steps:
      - uses: actions/checkout@v1

      - name: Cache node modules
        uses: actions/cache@v1
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-
      - name: Node ${{ matrix.node-version }}
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: npm install and npm run build
        run: |
          npm ci
          npm run build --base-href=/portfolio-dkn/ --configuration=production

      # - name: Angular Deploy gh-pages Actions
      #   uses: AhsanAyaz/angular-deploy-gh-pages-actions@v1.3.2
      #   with:
      #     github_access_token: ${{ secrets.ACCESS_TOKEN }} # see the Configuration section for how you can create secrets
      #     #build_configuration: staging # The build environment for the app. please look configurations in your angular.json
      #     base_href: /portfolio-dkn/   # make sure this corresponds to https://<your_username>.github.io/<base_href>/
      #     deploy_branch: gh-pages # The branch the action should deploy to.
      #     angular_dist_build_folder: dist/portfolio-donna # The folder where your project is supposed to be after running ng build by the action.

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist/portfolio-donna # The folder the action should deploy.
          token: ${{ secrets.ACCESS_TOKEN }}
          branch: gh-pages
          clean: true
          force: true


      # - name: Push Build to Releases
      #   uses: ncipollo/release-action@v1
      #   with:
      #     artifacts: "dist/portfolio-donna/*"
      #     token: ${{ secrets.TOKEN }}


          
  # Deployment job
  # deploy:
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v1